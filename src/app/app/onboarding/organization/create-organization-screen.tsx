"use client";

/**
 * Placeholder screen — AppAuthGate renders CreateOrganizationForm when the
 * user has no organization. This page exists so the route is valid.
 */
export function CreateOrganizationScreen() {
  return null;
}
